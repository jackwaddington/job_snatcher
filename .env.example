# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=job_snatcher
POSTGRES_PASSWORD=changeme
POSTGRES_DB=job_snatcher_dev

# Airflow
AIRFLOW__CORE__DAGS_FOLDER=./src/airflow_dags
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__LOAD_EXAMPLES=False
AIRFLOW__CORE__UNIT_TEST_MODE=False

# LLM — which backend to use for cover letter generation and repo summaries
# Options: claude | ollama_local | ollama_gaming
# ollama_local is the default dev mode — no API key needed
GENERATOR_LLM=ollama_local

# Claude API (production — only needed when GENERATOR_LLM=claude)
CLAUDE_API_KEY=your-key-here
CLAUDE_MODEL=claude-opus-4-6

# Ollama — gaming PC (used by reasoning matcher + optionally generator)
OLLAMA_BASE_URL=http://gaming-pc-local-ip:11434
OLLAMA_MODEL=neural-chat

# Ollama — local Mac (used for dev when GENERATOR_LLM=ollama_local)
LOCAL_OLLAMA_URL=http://localhost:11434
LOCAL_OLLAMA_MODEL=llama3.2

# Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/...

# System
GAMING_PC_MAC_ADDRESS=aa:bb:cc:dd:ee:ff
MATCH_SCORE_THRESHOLD=0.65
